# Test Data

The data files in here area available for use in the docker containers providing
services for testing (e.g. Postgres database).

They are also available for the unit test cases via a fixture `td` that provides
the Path for this director. So this README file would be `td / 'README.md`.
